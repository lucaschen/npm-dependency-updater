'use babel';

import NpmDependencyUpdaterView from '../lib/npm-dependency-updater-view';

describe('NpmDependencyUpdaterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
